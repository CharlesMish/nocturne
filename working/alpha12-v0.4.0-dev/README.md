# Alpha.12 / v0.4.0-dev evidence status

Status: **implementation and verification pending**

This transition introduces:

- one shared source tree;
- Nocturne and Nocturne Pi presentation profiles;
- server-only versus local-display as a diagnostic deployment dimension;
- a product-first `main` branch and version-matched `evidence` branch;
- dual deterministic product archives planned from one build identity.

Do not copy alpha.11 verification results forward as alpha.12 results. Codex
should populate this folder only after executing the new profile and release
checks against final extracted archives.

Product commit: `PENDING`
Nocturne archive SHA-256: `PENDING`
Nocturne Pi archive SHA-256: `PENDING`
Evidence archive SHA-256: `PENDING`
