# Provenance pointer

Detailed source-page screenshots are stored in the version-matched `evidence`
branch rather than the normal product tree. Runtime source, license, filename,
edit, size, and SHA-256 metadata remain in `sounds/sound_library.json`,
`AUDIO_CREDITS.md`, and `AUDIO_PROVENANCE.md`.
