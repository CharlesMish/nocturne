# Unverified-provenance quarantine

The historical `soft-wind-trees.mp3` candidate is **not shipped** here. Its local payload
did not audibly match the cited Freesound source, so Nocturne retains only the audit
record and historical SHA-256 in `sounds/sound_library.json` under `excluded_sounds`.
No CC0 claim is made for that missing local payload.
