# Seam-risk quarantine

Files in this directory are evidence and repair inputs, not assignable Nocturne sounds.
FastAPI denies the entire `sounds/inbox/` subtree, and the release audit fails if an
entry here appears in `default_slots` or the public `sounds[]` catalog.

## `rain-inside-house.mp3`

Numerical screening found an extreme tail-to-head discontinuity (boundary-to-internal
adjacent-change ratio approximately 51.9). That is a strong click/seam risk, not a
human listening verdict. Preserve the file and provenance, but do not return it to the
picker until it is repaired and auditioned on the intended devices.
